{% extends 'back/base.html.twig' %}

{% block body %}
    <h1>Modifier le Genre</h1>

    {{ include('back/gender/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_admin_gender_index') }}">Retour à la liste</a>

    {{ include('back/gender/_delete_form.html.twig') }}
{% endblock %}
