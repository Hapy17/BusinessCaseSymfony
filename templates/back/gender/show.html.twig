{% extends 'back/base.html.twig' %}

{% block body %}
    <h1>Genre</h1>

    <table class="table table-hover table-dark table-striped ">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gender.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ gender.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_gender_index') }}">Retour à la liste</a>

    <a href="{{ path('app_admin_gender_edit', {'id': gender.id}) }}">Modifier</a>

    {{ include('back/gender/_delete_form.html.twig') }}
{% endblock %}
